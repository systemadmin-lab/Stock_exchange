import { Button } from '@/components/ui/button';
import { memo } from 'react';

const Page = () => {
  return (
    <div className='flex justify-center h-screen home-wraper'>
      home
      
    </div>
  );
};

export default memo(Page);